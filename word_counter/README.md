# Word Counter

Takes in a text file as a command line flag and counts how many times each word occurs, printing the results in alphabetical order.

## How to Use

You can run this program by doing the following

```go run word_count.go -file "words.txt"```